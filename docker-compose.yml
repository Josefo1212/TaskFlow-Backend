services:
  proxy:
    container_name: taskflow-proxy
    image: nginx:1.27-alpine
    restart: unless-stopped
    command: /bin/sh -c "while :; do sleep 6h & wait $$!; nginx -s reload; done & nginx -g 'daemon off;'"
    depends_on:
      - gateway-api
    ports:
      - "80:80"
    volumes:
      - ./proxy/nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - edge

  db:
    container_name: tasksflow_db
    image: postgres:18-alpine
    restart: unless-stopped
    env_file:
      - ./db/.env.docker
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - backend

  redis:
    container_name: taskflow_redis
    image: redis:8-alpine
    restart: unless-stopped
    ports:
      - "127.0.0.1:6379:6379"
    networks:
      - backend

  auth-service:
    container_name: auth-service
    image: josefo1212/auth-service:3.2.0
    restart: unless-stopped
    depends_on:
      - db
      - redis
    env_file:
      - ./auth-service/.env
      - ./auth-service/.env.docker
    expose:
      - "3001"
    networks:
      - backend

  gateway-api:
    container_name: gateway-api
    image: josefo1212/gateway-api:2.3.0
    restart: unless-stopped
    depends_on:
      - auth-service
      - project-service
      - task-service
      - user-service
    env_file:
      - ./gateway-api/.env
      - ./gateway-api/.env.docker
      - ./gateway-api/.env.prod.docker
    expose:
      - "3003"
    networks:
      - edge
      - backend

  task-service:
    container_name: task-service
    image: josefo1212/task-service:1.0.0
    restart: unless-stopped
    depends_on:
      - db
    env_file:
      - ./task-service/.env
      - ./task-service/.env.docker
    expose:
      - "3004"
    networks:
      - backend

  project-service:
    container_name: project-service
    image: josefo1212/project-service:1.0.0
    restart: unless-stopped
    depends_on:
      - db
    env_file:
      - ./project-service/.env
      - ./project-service/.env.docker
    expose:
      - "3006"
    networks:
      - backend

  user-service:
    container_name: user-service
    image: josefo1212/user-service:1.0.0
    restart: unless-stopped
    depends_on:
      - db
    env_file:
      - ./user-service/.env
      - ./user-service/.env.docker
    expose:
      - "3002"
    networks:
      - backend

networks:
  edge:
  backend:

volumes:
  db_data:
